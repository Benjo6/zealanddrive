<Page
    x:Class="ZealandDrive.View_Dk.PassagerOversigt"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ZealandDrive.VM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <local:RouteVM/>
    </Page.DataContext>
    
    
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Content="Load" Width="1000" HorizontalContentAlignment="Center"  VerticalContentAlignment="Center" Command="{Binding LoadCurrentPas}"/>
        <StackPanel Grid.Column="1" Grid.Row="1" Grid.RowSpan="3" Margin="15">
            <TextBlock Text="Alle Brugere" Margin="50,0,0,0" FontWeight="Bold" FontSize="14" ></TextBlock>
            <ListView ItemsSource="{Binding Passengers}" SelectedItem="{Binding SelectedPassenger, Mode=TwoWay }">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding id}" Width="20"></TextBlock>
                            <TextBlock Text="{Binding routeId}" Width="100" Margin="20,0,0,0"></TextBlock>
                            <TextBlock Text="{Binding userId}"  Width="40" Margin="20,0,0,0"></TextBlock>
                            <TextBlock Text="{Binding status}"  Width="40" Margin="20,0,0,0"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        
        <Button Grid.Row="3" Grid.Column="1" Content="Accepter" />
        <Button Grid.Row="3" Grid.Column="1" Margin='100,10,1,10' Content="Afvis"/>
    </Grid>
</Page>
